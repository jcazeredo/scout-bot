name: Resume Render Service (Manual)

on:
  workflow_dispatch:

jobs:
  call-shared:
    uses: ./.github/workflows/render-control.yml
    with:
      action: resume
    secrets:
      RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}