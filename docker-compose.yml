version: '3.8'

services:
  scout-bot:
    build: .
    container_name: scout-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app:ro
